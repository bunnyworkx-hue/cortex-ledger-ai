[project]
name = "axiom-api"
version = "0.1.0"
description = "Axiom OS API — the Axiom control-plane HTTP surface"
requires-python = ">=3.11"
dependencies = [
    "axiom-core",
    "axiom-db",
    "axiom-anthropic",
    "axiom-graphify",
    "axiom-agent-fabric",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[tool.uv.sources]
axiom-core = { workspace = true }
axiom-db = { workspace = true }
axiom-anthropic = { workspace = true }
axiom-graphify = { workspace = true }
axiom-agent-fabric = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["axiom_api"]
